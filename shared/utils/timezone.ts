// Every displayed or compared time is resolved in the event's timezone rather
// than the host's, so a server in UTC and a phone in another country agree on
// what "18:00" means
export const TIMEZONE = "Europe/Helsinki";
